{% extends "_layout.twig" %}

{% set title = thread.title %}

{% if log %}
	{% set actions = {("newreply.php?id="~id) : "New reply"} %}
{% endif %}

	{% block content %}
{% for post in posts %}
	{{ threadpost(post) }}
{% endfor %}
	{% endblock %}
