{% extends "_layout.twig" %}

{% set title = "New thread" %}
{% set noindex = true %}

	{% block content %}
{% if error %}
	<p class="error">The following errors occured when trying to make a new thread: <br>- {{ error | join('<br>- ') | raw }}</p>
{% endif %}

{% if action == 'Preview' %}
	{{ threadpost(post) }}
{% endif %}

{{ postform('', 'New thread', threadtitle, message, true) }}
	{% endblock %}