{% extends "_layout.twig" %}

{% set title = "Edit category" %}
{% set noindex = true %}

{% set breadcrumb = {"manageforums.php": "Forum management"} %}

	{% block content %}
<form action="" method="POST">
	<table class="c1">
		<tr class="h"><td colspan="2">{% if cid == 'new' %}Create{% else %}Edit{% endif %} category</td></tr>
		<tr>
			<td class="n1 center">Title:</td>
			<td class="n2"><input type="text" name="title" value="{{ cat.title }}" size="50" maxlength="500"></td>
		</tr><tr>
			<td class="n1 center">Display order:</td>
			<td class="n2"><input type="text" name="ord" value="{{ cat.ord }}" size="4" maxlength="10"></td>
		</tr>
		<tr class="h"><td colspan="2">&nbsp;</td></tr>
		<tr>
			<td class="n1 center"></td>
			<td class="n2">
				<input type="submit" name="savecat" value="Save category">
				{% if cid != 'new' %}
					<input type="submit" name="delcat" value="Delete category" id="delcat">
				{% endif %}
				<button type="button" id="mfback">Back</button>
			</td>
		</tr>
	</table>
</form>
	{% endblock %}